<div class="container">
    <div class="row">
        <div class="col-md-7">
        <div class="main_image_size">
            <%= image_tag @portfolio_item.main_image.url, width: '100%' %> 
             <%# <%= image_tag portfolio_img(@portfolio_item.main_image, 'main'), width: '100%' %> 
        </div>
        </div>
        <div class="col-md-5">
            <h1><%= @portfolio_item.title %></h1>

            <em><%= @portfolio_item.subtitle %></em>

            <p><%= @portfolio_item.body %></p>

            <%# <h2>Technologies Used:</h2> %>

            <%# <% @portfolio_item.technologies.each do |f| %>
            <%# <p><%= f.name %></p>
            <%# <% end %> 
        </div>
    </div>

</div>





